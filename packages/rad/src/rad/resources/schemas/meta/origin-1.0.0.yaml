%YAML 1.1
---
$schema: asdf://stsci.edu/datamodels/roman/schemas/rad_schema-1.0.0
id: asdf://stsci.edu/datamodels/roman/schemas/meta/origin-1.0.0

title: Institution / Organization Name
description: |
  The name of the institution or organization
  responsible for creating this file.

type: string
enum: ["STSCI", "STSCI/SOC", "IPAC/SSC"]

sdf:
  special_processing: VALUE_REQUIRED
  source:
    origin: TBD
archive_catalog:
  datatype: nvarchar(15)
  destination:
    [
      WFIExposure.origin,
      GuideWindow.origin,
      WFIMosaic.origin,
      SourceCatalog.origin,
      SegmentationMap.origin,
    ]
